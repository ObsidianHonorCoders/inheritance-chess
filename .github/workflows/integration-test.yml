name: integration-test

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  build-test-and-run:
    # REMOVED: The 'if' condition is no longer needed because
    # we removed the trigger that caused the problem.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          clean: true
      - name: Toolchain preflight
        run: |
          cmake --version
          ctest --version

      - name: Compiler preflight (non-Windows)
        if: runner.os != 'Windows'
        run: c++ --version
      - name: Config and Build Script
        run: cmake -DBUILD_TESTS=ON -DCLEAN_BUILD=ON -P cmake/configure_and_build.cmake
      - name: Run Tests Script
        run: cmake -P cmake/run_tests.cmake
      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: diagnostics-${{ matrix.os }}
          if-no-files-found: ignore
          path: |
            build/**/Testing/**
            build/**/Test.xml
            build/**/CMakeOutput.log
            build/**/CMakeError.log
